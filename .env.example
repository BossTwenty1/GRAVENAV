# GRAVENAV environment template.
# Real secrets and local configuration belong in .env.
# .env must never be committed.

# Public Supabase project configuration used by the browser and server helpers.
# Leave blank until a project is provisioned; never place service-role keys here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
